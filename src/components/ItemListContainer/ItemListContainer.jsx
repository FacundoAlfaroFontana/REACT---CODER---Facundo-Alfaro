const ItemListContainer = ({greeting}) => {
    return (
        <div style={{fontSize: `2rem`, fontWeight: "bold", textAlign: "center"}}>{greeting}</div>
    );
};

export default ItemListContainer;
